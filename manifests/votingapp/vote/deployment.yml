apiVersion: v1
kind: Service
metadata:
  name: vote-app
  annotations:
    kubernetes.io/change-cause: "Initial creation of redis service"
  namespace: votingapp
  labels:
    app: vote-app-fe
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    app: vote-app-fe
---